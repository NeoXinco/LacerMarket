<p>Salve <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>Ti stiamo contattando per informarti che la tua email è stata cambiata <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p>Ti stiamo contattando per informarti che la tua email è stata cambiata <%= @resource.email %>.</p>
<% end %>
